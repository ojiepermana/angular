/*
 * Public API Surface of @ojiepermana/material (primary entry)
 *
 * Components and utilities are shipped via subpath entries:
 *   - `@ojiepermana/material/theme`      — design tokens, CSS variables, `ThemeService`, `provideMaterialTheme`
 *   - `@ojiepermana/material/shadcn`     — shadcn/ui primitives on top of Material M3
 *   - `@ojiepermana/material/layout`     — layout primitives (WIP)
 *   - `@ojiepermana/material/navigation` — navigation primitives (WIP)
 *
 * Consumers should import from the subpaths directly.
 */

export const MATERIAL_VERSION = '0.0.1';
